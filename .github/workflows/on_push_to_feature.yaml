name: Run tests

on:
  push:
    branches:
      - feature/**
  workflow_dispatch:

jobs:
  feature_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Run tests (placeholder)
        run: echo running tests...
